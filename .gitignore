node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
.data/

# Seeded design canvas — a ~2MB generated bundle, rebuilt from the .dc.html
# artboards and canvas.json beside it. The sources are tracked; this is not.
.design/shift-story-landing.html

# Walkthrough output — screenshots, clips and the stitched video are produced
# by `npm run demo` and regenerated on every run.
demo-output/

# Netlify build/runtime output — not user code.
.netlify/
